# Frontend environment (copy to .env and adjust)
#
# Leave empty to use the Vite dev proxy (/api -> http://localhost:8000).
# For production point this at your deployed backend, e.g.:
#   https://your-backend.onrender.com/api/v1
VITE_API_URL=
